<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-lg-4 col-md-6">
                <div class="single-blog">
                    <div class="blog-img">
                        <img src="@item.Img" alt="blog image" />
                    </div>
                    <div class="blog-content">
                        <div class="blog-title">
                            <h4><a asp-controller="Blog" asp-action="Post" asp-route-id="@item.Id">@item.Name</a></h4>
                            <div class="meta">
                                @item.Date
                            </div>
                        </div>
                        <p>
                            @item.PrewText
                        </p>
                   
                        <a class="box_btn"  asp-controller="Blog" asp-action="Post" asp-route-id="@item.Id" >read more</a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
